# entrada de dados (inputs)
precoKilo = 12
pesoPrato = float(input("Informe o peso da refeição: "))

# processamento = lógica
precoFinal = precoKilo * pesoPrato

# saída de dados (output)
print("Consumo do cliente:", precoFinal)